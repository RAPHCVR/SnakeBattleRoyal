apiVersion: v1
kind: Service
metadata:
  name: snake-duel-client
  labels:
    app.kubernetes.io/name: snake-duel-client
    app.kubernetes.io/component: client
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: snake-duel-client
  ports:
    - name: http
      port: 80
      targetPort: http
