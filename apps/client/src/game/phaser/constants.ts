import { DEFAULT_GAME_CONFIG } from "@snake-duel/shared";

export const CELL_SIZE_PX = 32;
export const GRID_WIDTH = DEFAULT_GAME_CONFIG.width;
export const GRID_HEIGHT = DEFAULT_GAME_CONFIG.height;
export const WORLD_WIDTH_PX = GRID_WIDTH * CELL_SIZE_PX;
export const WORLD_HEIGHT_PX = GRID_HEIGHT * CELL_SIZE_PX;
export const FOOD_PARTICLE_TEXTURE = "food-particle";

